on:
  push:

env: 
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build and run sussg
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: cargo build --release --verbose
      - run: cargo test
